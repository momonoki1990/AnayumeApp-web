<div class="container text-white">
<div class="row">
<div class="col-md-6">
<section class="user_info mt-5">
  <h1 class="prof-name"><%= current_user.name %></h1>
</section>
<section class="stats">
  <%= render 'shared/stats' %>
</section>
<%= render 'follow_form' if logged_in? %>
</div>
<div class="col-md-6 mt">
<% if @feed_items.any? %>
<h4>投稿(<%= @feed_items.count %>)</h4>
<%= render 'shared/feed' %>
<% end %>
</div>
</div>
</div>