<div class="row">
  <div class="col-4 pr-0">
    <%= render 'shared/user_img', user: @user %>
  </div>
  <div class="col-7 offset-1">
    <div class="prof-name h4">
      <%= link_to @user.name, @user, class: 'link-white' %>
    </div>
    <section class="stats">
      <%= render 'shared/stats' %>
    </section>
    <section class="follow-form">
      <%= render 'users/follow_form' if logged_in? %>
    </section>
  </div>
</div>